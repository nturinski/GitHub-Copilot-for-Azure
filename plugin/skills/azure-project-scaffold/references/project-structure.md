# Canonical Project Structures

> Read during **Phase 1, Step P3** (to fill plan Section 5) and **Phase 2, Step 1** (to create directories). Pick the tree matching the plan's runtime + app type.

---

## TypeScript — SPA + API (default)

```
project-root/
├── .azure/
│   ├── project-plan.md
│   └── execution-checklist.md     ← created during scaffold
├── .env.example
├── .gitignore
├── package.json                    ← Root workspace config
├── src/
│   ├── functions/                  ← Azure Functions project
│   │   ├── host.json
│   │   ├── local.settings.json
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── src/
│   │   │   ├── functions/          ← One handler per file
│   │   │   ├── services/           ← Service abstraction layer
│   │   │   │   ├── interfaces/     ← Service contracts
│   │   │   │   ├── config.ts       ← Config loader + validation
│   │   │   │   └── registry.ts     ← Service factory / DI
│   │   │   ├── errors/             ← Error types and middleware
│   │   │   ├── middleware/
│   │   │   └── utils/              ← Shared helpers (DRY)
│   │   ├── tests/                  ← Generated by azure-project-verify
│   │   └── seeds/
│   │       ├── fixtures/
│   │       ├── migrations/
│   │       └── seed.ts
│   ├── web/                        ← Frontend (if applicable)
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   └── src/
│   │       ├── api/client.ts       ← Typed API client (post Step 11)
│   │       ├── auth/
│   │       ├── components/
│   │       ├── hooks/
│   │       ├── pages/
│   │       ├── types/              ← Local mock types (preview only)
│   │       └── mocks/              ← Mock data + API client (preview only)
│   └── shared/                     ← Shared types and schemas
│       ├── package.json
│       ├── tsconfig.json
│       └── src/
│           ├── types/
│           │   ├── entities.ts     ← Entity types
│           │   └── api.ts          ← Response contracts + ErrorCode
│           └── schemas/
│               └── validation.ts   ← Zod schemas + inferred request types
```

### TypeScript — API only

Same as above minus `src/web/`.

---

## C# (.NET 10 isolated) — SPA + API

```
project-root/
├── .azure/
├── .env.example
├── ProjectName.sln
├── src/
│   ├── Functions/
│   │   ├── ProjectName.Functions.csproj
│   │   ├── Program.cs              ← Isolated worker host
│   │   ├── host.json
│   │   ├── local.settings.json
│   │   ├── Functions/              ← One handler per file
│   │   ├── Services/
│   │   │   ├── Interfaces/
│   │   │   ├── Config.cs
│   │   │   └── ServiceRegistry.cs
│   │   ├── Errors/
│   │   └── Middleware/
│   ├── Web/                        ← Blazor or separate JS frontend
│   └── Shared/
│       ├── ProjectName.Shared.csproj
│       ├── Entities/
│       ├── Contracts/
│       └── Validation/             ← FluentValidation
└── tests/
```

---

## Background Worker (no HTTP frontend)

Same as the runtime's API-only tree but `src/functions/src/functions/` contains queue-/timer-/event-trigger handlers instead of HTTP routes. No `src/web/`.

---

## Files NOT Created by This Skill

This skill is **infra-aware but IaC-free**. It declares deployment intent in `.azure/project-plan.md` Section 4a and stops there. Do NOT generate any of the following:

| File / Path | Created By |
|-------------|-----------|
| `infra/main.bicep`, `infra/**/*.bicep` | `azure-prepare` |
| `infra/main.tf`, `infra/**/*.tf` | `azure-prepare` |
| `azure.yaml` | `azure-prepare` |
| `Dockerfile` (any component) | `azure-prepare` |
| `.github/workflows/*.yml` (deployment pipelines) | `azure-prepare` |
| `.azure/deployment-plan.md` | `azure-prepare` |
| `docker-compose.yml`, IDE debugger launch config (e.g., `.vscode/launch.json`) | `azure-localdev` |

`azure-prepare` reads `.azure/project-plan.md` Section 4a (Required Resources + User-Selected Resources) as its initial service inventory \u2014 see [azure-services-catalog.md](azure-services-catalog.md) for the bridge contract.
